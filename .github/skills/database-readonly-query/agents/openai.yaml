display_name: Database Readonly Query
short_description: Run safe read-only queries and schema discovery against approved test databases.
default_prompt: Use database-readonly-query to inspect the default test database schema and validate the answer with real rows.
