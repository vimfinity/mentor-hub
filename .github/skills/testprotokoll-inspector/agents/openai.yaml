interface:
  display_name: "Testprotokoll Inspector"
  short_description: "Analyze German QA Word test protocols"
  default_prompt: "Use $testprotokoll-inspector to extract and inspect the QA test protocol at the provided .docx path."

policy:
  allow_implicit_invocation: true
