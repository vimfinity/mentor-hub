{
  "name": "mentor-hub",
  "version": "1.0.0",
  "description": "KI-Hub — a zero-dependency Node.js content hub with surveys, a curated feed, and an admin console.",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/",
    "lint": "node scripts/lint.js",
    "optimize:images": "node scripts/optimize-images.js"
  },
  "engines": {
    "node": ">=18"
  }
}
